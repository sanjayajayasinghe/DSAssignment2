import { RowToFilterPipe } from './row-to-filter.pipe';

describe('RowToFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RowToFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
